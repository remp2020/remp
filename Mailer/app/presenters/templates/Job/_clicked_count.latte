{foreach $job->related('mail_job_batch_templates') as $jobBatchTemplate}
    <span class="actions">
        {var $percent = $jobBatchTemplate->sent ? (100 * $jobBatchTemplate->clicked / $jobBatchTemplate->sent) : 0}
        {$jobBatchTemplate->clicked} ({$percent|number:0}%)
    </span>
{/foreach}
