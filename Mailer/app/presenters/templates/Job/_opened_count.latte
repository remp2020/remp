{foreach $job->related('mail_job_batch_templates') as $jobBatchTemplate}
    <div>
        {var $percent = $jobBatchTemplate->sent ? (100 * $jobBatchTemplate->opened / $jobBatchTemplate->sent) : 0}
        {$jobBatchTemplate->opened} ({$percent|number:0}%)
    </div>
{/foreach}
